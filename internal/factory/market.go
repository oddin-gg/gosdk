package factory

// Phase 6.1 reshape: the previous market/outcome impl wrappers
// (marketImpl, marketWithOddsImpl, marketWithSettlementImpl,
// marketCancelImpl, outcomeOddsImpl, outcomeSettlementImpl) are gone.
// Markets are now value structs constructed directly in markets.go.
